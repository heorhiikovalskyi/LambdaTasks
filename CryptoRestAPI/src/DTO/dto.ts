export type CurrencyAverage = { "average price (USD)": number | null };

export type CurrenciesAverage = { "average price (USD)": number | null; symbol: string | null };

export type MarketExchanges = { market: string; currency: string; price: number };
